#use command :
    > git clone <repository_url> 
    > cd project-directory
    > composer install
    > copy .env.example .env
    > php artisan key:generate
    > php artisan migrate
    > php artisan db:seed
    > npm install
    > npm run dev
    > php artisan serve
    
